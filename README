Compile command gcc -o forked_fib_b forked_fib_b.c
Compile command gcc -o forked_fib_i forked_fib_i.c
Compile command gcc -o forked_fib_r forked_fib_r.c

Start-up: ./forked_fib_b
Start-up: ./forked_fib_i
Start-up: ./forked_fib_r

Project Overview:
    Write a C program using the fork( ) system call that generates the Fibonacci sequence in the
    child process. The number of the sequence will be provided in the command line. For example, if 5
    is provided, the first five numbers in the Fibonacci sequence will be output by the child process.
    Because the parent and child processes have their own copies of the data, it will be necessary for the
    child process to output the sequence. Have the parent invoke the wait( ) call to wait for the child
    process to complete before exiting the program. Perform necessary error checking to ensure that a
    non-negative number is passed on the command line.

Main Objective:
    The main objective of this programming project is to get familiar with creating a
    process using the fork( ) system call in Linux.

Files
    Fib_with-Forks/
    |--forked_fib_b.c
    |--forked_fib_i.c
    |--forked_fib_r.c
    |--README.txt

Other Related Folders
    Fibonacci
    Forks/Child_Processes


forked_fib_b.c
-Contains both the iterative and recursive Fibonacci solutions that generates in the child process


fibonacci_iterative.c
-Contains only the iterative Fibonacci solution that generates in the child process

fibonacci_recursive.c
-Contains only the recursive Fibonacci solution that generates in the child process



Luminokx - Find the light in darkness
